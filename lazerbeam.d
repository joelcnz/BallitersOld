
import std.stdio;
import unit, ship, unitlist, board;
import jeca.all;

class LazerBeam: Unit {
	this(Ship ship0) {
	}

	override void process(Board board, UnitList unitList) {
	}

	override void draw() {
	}
}
